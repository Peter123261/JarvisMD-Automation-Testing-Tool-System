# JarvisMD Automation Testing Tool - Environment Configuration
# OpenAI API Configuration
openai_api_key=sk-your-api-key-here
# Database Configuration
DATABASE_URL=sqlite:///./jarvismd_automation.db

# API Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Port Configuration (to avoid conflicts with other projects)
API_PORT=8010
POSTGRES_PORT=5433
REDIS_PORT=6380
FLOWER_PORT=5555

# Application Settings
LOG_LEVEL=INFO
MAX_CONCURRENT_EVALUATIONS=5

# Database name (keeping old name to preserve data)
POSTGRES_DB=medbench_automation

# CORS Configuration for Frontend Development
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# ============================================================================
# PostgreSQL Database Configuration
# ============================================================================
POSTGRES_DB=medbench_automation
POSTGRES_USER=medbench
POSTGRES_PASSWORD=medbench_secure_password_change_in_production
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# ============================================================================
# Redis Configuration
# ============================================================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=  # Leave empty for development, set in production

# ============================================================================
# Application Configuration
# ============================================================================
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
API_PORT=8000
DEBUG=true
LOG_LEVEL=INFO
RELOAD=true

# ============================================================================
# Metrics Configuration
# ============================================================================
API_METRICS_PORT=8007
API_METRICS_HOST=0.0.0.0
WORKER_METRICS_PORT=8002
WORKER_METRICS_HOST=0.0.0.0
# Optional: Override metrics URLs (leave empty to auto-construct from host/port)
# API_METRICS_URL=http://localhost:8007/metrics
# WORKER_METRICS_URL=http://celery-worker:8002/metrics
WORKER_METRICS_EXTERNAL_PORT=8006  # External port mapping for worker metrics

# ============================================================================
# Celery Configuration
# ============================================================================
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
CELERY_WORKER_CONCURRENCY=4
CELERY_WORKER_POOL=threads

# ============================================================================
# Flower Monitoring (Celery UI)
# ============================================================================
FLOWER_PORT=5555
FLOWER_USER=admin
FLOWER_PASSWORD=admin_secure_password_change_in_production

# ============================================================================
# Database URL (Auto-generated, don't modify)
# ============================================================================
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ============================================================================
# Application Settings
# ============================================================================
MAX_CONCURRENT_EVALUATIONS=5
MAX_FILE_SIZE=10485760  # 10MB
ALLOWED_FILE_TYPES=.txt,.json,.csv

# ============================================================================
# CORS Origins (comma-separated)
# ============================================================================
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# ============================================================================
# Production Settings (Only for production)
# ============================================================================
# HTTPS=true
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

   OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
   GRAFANA_URL=http://localhost:3000
   JAEGER_URL=http://localhost:16686









